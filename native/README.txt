Place an Android-compatible libneedle.so here when using the ctypes NativeRuntime.

The currently tested Android binary is the standalone executable:
$PREFIX/bin/needle-native

The Ubuntu/glibc libneedle.so must NOT be copied here for Android use.
